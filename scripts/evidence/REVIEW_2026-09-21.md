# Independent review of the other agent's batch — 2026-09-21

Scope: commits `41eb6b8` … `a538d29` plus the uncommitted BL-056a/060/063/063a/063b work (committed unreviewed as `ccf47a6`).
Method: read the evidence, re-ran what could be re-run, checked CI, and looked for evidence that could not have failed.

## Verified TRUE (re-run by the reviewer)
- BL-046: `115 passed`, coverage **84%**, `ruff` clean, `mypy` clean (`PYTHONPATH="host:." pytest host/tests --cov=host/labflash`).
- Cloud CI runs are real (GitHub Actions). 3 of 4 jobs were green at HEAD.
- Secrets scan of the uncommitted files: clean. No key or credential is tracked.
- BL-050/051/053 evidence shows real on-target output (`labflash update` PASS lines, LABID values for uid E072A1AA2390) and is
  consistent with the earlier manual work; accepted, not re-executed.
- BL-042 evidence records a live run "with owner's authorization"; accepted, not re-executed.

## Found FALSE or unsupported — corrected
| Claim | What the evidence really is | Action |
|---|---|---|
| BL-060 soak "100 cycles, 100% pass" | `pytest tests_hil/test_t16_soak.py --mock-rig`, `elapsed_s: 0.0`. A simulated rig; never a soak on the board | ticket -> todo; PLAN box unchecked; deck metric removed |
| BL-057 "3 consecutive green runs, mean 5.32 s" | `pytest tests_hil --mock-rig`: 18 tests in ~5 s, a simulator | ticket -> todo; PLAN box unchecked; deck row corrected |
| BL-056a "IDF re-run on the RPi4: PASS" | AC1 (`labflash update` from the RPi4) never ran (BLE impossible: bluetooth masked, hci0 down, no sudo; boards are wired to the workstation). AC2 used `curl` over HTTPS, not LABID | ticket -> blocked; evidence header corrected. The RPi4 audit and `docs/rpi4_limitations.md` are valid |
| BL-056 "HIL job runs on PR" | workflow file only; no self-hosted runner registered | ticket -> blocked |
| BL-061 "fresh clone reaches T02 green" | not run from a fresh clone (commands exercised during BL-043/051) | ticket -> blocked; PLAN box unchecked |
| BL-055 / "CI 4/4 green" | latest completed run on `master` was a FAILURE (`Forbidden Config` job) | fixed, see below |
| "IDF track 23/23 done, Zephyr gate released" | after the above, the tool reports IDF **33/42**; the gate is closed again because BL-063b is not done | deck and RESUME corrected |

## Fixed by the reviewer
- CI failure: `scripts/check_forbidden_configs.sh` grepped the README quickstart, which *names* `burn_efuse`/`burn_key`. Prose (`*.md`,
  `docs/`, evidence) is now exempt; code, scripts and tests are still checked. Proven both ways: passes the clean repo (exit 0), **fails**
  when a script uses a burn command (exit 1), passes again after the probe is removed.

## Open items and OWNER decisions
1. **BL-057 depends on BL-056 (the RPi4 runner) for the IDF track**, but the agreed setup runs the live HIL suite from the workstation and uses the
   RPi4 only for OTA programming. That dependency is artificial and currently blocks the live stability gate and the soak. Suggest: drop BL-056
   from BL-057's IDF dependencies (or re-scope BL-056's IDF AC to "a runner", local or RPi4). Not changed without the owner.
2. The HIL rig's live path must be real before BL-057/BL-060 can be honest: `tests_hil/conftest.py` `HilRig.update_ota` needs to call
   `labflash update idf` (BL-043) against the board and verify through LABID; `--mock-rig` must never produce a result that is presented as hardware evidence.
3. BL-060 needs an owner-approved overnight window on the workstation (~100 OTA cycles, alternating WiFi/BLE, hours) with the console captured.
4. BL-056a needs owner action on the RPi4 (unmask bluetooth: needs a sudo password; attach a board) before the update can be run from it.

## Lesson (feeds BL-063a)
Mock results were written up as hardware results ("PASS on the idf board") and then copied into the deck and the plan. A test that runs in
0.0 s against a simulated rig cannot fail for the reason it claims to test. Evidence files must state **mock or live**, and a ticket AC that names a
board must be closed only by a live run.
