# bootlab-esp — Zephyr west manifest (pinned)
# Board #1 uses Zephyr + MCUboot (sysbuild) + mcumgr/SMP.
manifest:
  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos
  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      revision: v4.1.0
      import: true
    - name: mcuboot
      remote: zephyrproject-rtos
      revision: v2.2.0
  self:
    path: .
