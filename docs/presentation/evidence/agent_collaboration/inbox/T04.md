# T04 — `docs/BL069_IMAGE_POOL.md`
Write the doc so that `host/tests/test_pinpolicy.py::test_doc_lists_every_group_and_every_safe_pin` passes:
it must contain every group name from `EXCLUDED_PIN_GROUPS` and `GPIO<n>` for every pin in `SAFE_OUTPUT_PINS`
(read `host/labflash/pinpolicy.py`; copy names/reasons verbatim, do not invent pins or reasons).

Sections: 1) Purpose (2-3 sentences: pool of signed images with footprint different from v1/v2, built offline in WSL).
2) Pin allowlist: one table `| Group | GPIOs | Reason |` for the excluded groups, plus a line listing safe pins as `GPIO4, GPIO5, ...`.
3) Pool composition (13 images: 9 valid, 2 valid+trailer i.e. unaligned size, 1 near_limit = exactly 4 MiB file, 1 too_big
= 4 MiB + 4096; expected outcome per transport as in `poolmanifest.expected_outcomes`). 4) Manifest schema: table of every key
in `poolmanifest.make_entry` and the top-level keys. 5) Budget placeholder line: "~3.5 h single overnight run, BLE budgeted at the
drifted ~280 s/cycle" (from RESUME/Trap 24; do not compute anything else). 6) Note that `.bin` files are gitignored, only manifest/README/seed are committed.
Plain markdown, no emoji, no invented measurements.
