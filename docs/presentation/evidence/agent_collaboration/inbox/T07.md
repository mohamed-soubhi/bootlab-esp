# T07 — write `docs/BL067_RANDOM_SOAK.md` (runbook)
Docs only; this is the ONLY file you write. Read these first (all in this worktree): `tests_hil/soak_random.py` (the runner; its module
docstring and `main()` list every flag), `tests_hil/soak_model.py` (picker + outcome model), `docs/BL060_SOAK_TEST.md` (style to match),
`docs/BL069_IMAGE_POOL.md`. Use ONLY facts found in those files or listed here; invent no numbers.

Sections (match BL060_SOAK_TEST.md's tone; tables where it helps):
1. Purpose: 200-cycle randomized soak on board 1: seeded random image + transport per cycle, pass condition = the model's expected outcome.
2. Images: fixed valid v1..v4 (versions 1.0.0..4.0.0), failure images bad_sig / hang / no_confirm, generated pool (BL-069 manifest, not too_big).
   Mix 70 % fixed / 10 % generated / 20 % failure with a cap on consecutive failure images (`--failure-cap`, default 2). Transport by `--ble-share` (default 0.5).
3. Outcome model: table of image kind -> what the board must show afterwards (installs; rejected; rolls_back for no_confirm and hang) — copy from
   `soak_model.expected_after`. Explain warm-up (one WiFi and one BLE install before cycle 1, so a reject is never trusted over an unproven link).
4. How a cycle is checked (from `soak_random._check`): installs / rejected / no_confirm (pending unconfirmed state read, then reset, then rollback) / hang.
5. Determinism: the seed, `--seed`, `--dry-run`, and what makes a plan replayable; `--resume` must use the same seed, cycles, ble-share, failure-cap and pool (the header record enforces it).
6. Running it: exact command for native Windows (copy the example from the module docstring), the evidence files in `--out` (cycles.jsonl, status.json,
   report.json, update.log, console.log) and what each report.json field means (read `_report`).
7. Time budget: BLE ~280 s/cycle at the drifted rate and WiFi ~32 s/cycle (docs/LESSONS_LEARNED.md Trap 24), failure cycles slower; at 50 % BLE 200 cycles is many hours,
   so plan windows and use `--resume`. Do not compute a precise total.
8. Pass criteria: pass rate >= 99 %, not aborted, final board state confirmed (exit code 0). Abort rule: 3 consecutive unexpected outcomes.
9. Hardware rules: native Windows only (R14), never open COM from WSL, restore v1 at the end, evidence lives on the Windows side until copied into the repo.
Plain markdown, no emoji.
