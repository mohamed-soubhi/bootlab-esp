# T04 — `docs/BL069_IMAGE_POOL.md`

Status: DONE. `test_doc_lists_every_group_and_every_safe_pin` passes and all 71 BL-069 unit tests are green.
No test file, no `host/labflash/*` module touched.

## Files changed
- `docs/BL069_IMAGE_POOL.md` (new, 91 lines) — sections 1 purpose, 2 pin allowlist (7-group table copied verbatim
  from `pinpolicy.EXCLUDED_PIN_GROUPS` + all 19 `SAFE_OUTPUT_PINS` as `GPIO<n>`), 3 pool composition (13 images),
  4 manifest schema (top-level + every `make_entry` key), 5 budget placeholder, 6 committed artifacts.

## Command output (last lines)

    $ PYTHONPATH=host:. /home/msoubhi/bootlab-esp/.venv/bin/python -m pytest host/tests/test_pinpolicy.py -q
    ..........................                                               [100%]
    26 passed in 0.05s

    $ PYTHONPATH=host:. /home/msoubhi/bootlab-esp/.venv/bin/python -m pytest host/tests/test_pinpolicy.py \
        host/tests/test_imagegen.py host/tests/test_poolmanifest.py tests_hil/test_pool_schedule_unit.py -q
    .......................................................................  [100%]
    71 passed in 1.77s

ruff/mypy: not run, no Python file changed in this task (markdown only).

## Deviations
- None in the doc content. Group names, pin numbers and reasons are copied from `host/labflash/pinpolicy.py`
  as it currently stands (the `rgb_led_alt` entry for GPIO38 and the `jtag_mtxx` GPIO39-42 group are both included);
  no pin or reason was invented. Counts in section 3 come from `imagegen._POOL_ROLES` (9 valid, 2 unaligned_trailer,
  1 near_limit, 1 too_big) and the sizes from `imagefmt.SLOT_SIZE` / `SECTOR`.
- Sizes written as `4194304` and `4 MiB + 4096`: arithmetic on `SLOT_SIZE`/`SECTOR`, not measured values.

## Doubt for the lead (blocking for section 6, not for the test)
Section 6 states the `.bin` files are gitignored, per the task text. That is **not currently true**: `.gitignore`
has no `*.bin` (or pool-directory) rule — its labflash block ends at `coverage/` and the last entries are
`tests_hil/reports/`. As written, the doc documents an invariant the repo does not yet enforce, and the images
would show up as untracked.

I did not edit `.gitignore`, because the task named only the doc file. Suggested one-line fix for the lead, in the
labflash block:

    # BL-069 generated image pool (rebuild from the seed; see docs/BL069_IMAGE_POOL.md)
    *.bin

Say the word and I will add it (and reword section 6 if the pool is instead ignored by directory).

Stopping here per protocol; not starting T05 (none exists — T04 is the last inbox task).
