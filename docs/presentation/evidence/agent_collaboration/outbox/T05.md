# T05 — outbox report

## Status
Done.

## Files changed
- `docs/LESSONS_LEARNED.md` — appended Trap 25 … Trap 30 (lines 289-338), inserted after Trap 24 and before
  `## 5. Zephyr Track Status Summary`. This is the only file touched, as the task allows.

## Layout produced
- `### Trap 25: Signed images are always a multiple of 4096 bytes (BL-069, 2026-09-25)` — line 289
- `### Trap 26: A post-signature trailer is invisible to the bootloader but \`espsecure\` refuses it (BL-069, 2026-09-25)` — line 298
- `### Trap 27: An over-limit image fails in three places and the BLE symptom is silent (BL-069, 2026-09-25)` — line 307
- `### Trap 28: IDF's own size check refuses an over-partition build, and its error is on stdout (BL-069, 2026-09-25)` — line 315
- `### Trap 29: Image size grows in 64 KiB steps, so an exact byte target is not reachable (BL-069, 2026-09-25)` — line 323
- `### Trap 30: Same seed, same image: compare \`content_sha256\`, not \`sha256\` (BL-069, 2026-09-25)` — line 330

Heading format matches Trap 24. Bullets use the three labels the task names (`**Symptom/Observation**`, `**Cause**`,
`**Resolution / How to avoid**`) rather than Trap 24's own labels, since the task specifies them explicitly.
Trap block lengths: 9, 9, 8, 8, 7, 8 lines (incl. heading) — all inside the 5-9 range.

## Test / lint commands
None applicable: docs-only Markdown, no source file changed.
- pytest: not run (no `.py` file in scope for this task)
- ruff: not run (no Python file in scope)
- mypy: not run (no Python file in scope)

## Facts used
Only the facts listed in `.agent/inbox/T05.md`; no extra numbers, paths or claims added. Trap 26 states plainly
that it is NOT YET CONFIRMED ON HARDWARE and that Gate R1 is still pending, as the task requires.

## Deviations / doubts
1. The task said Trap 24 is "at the bottom of the file"; it is not — `## 5. Zephyr Track Status Summary` and
   `## 7. Owner Review & Sign-Off` follow it. Inserted the new traps directly after Trap 24 so the trap list stays
   contiguous, which preserves the stated intent.
2. Trap 30 bundles two facts (content_sha256 vs sha256, and the relative `--out` path bug). Kept them in one trap
   with the second framed as a related finding, since the task lists both under Trap 30.
3. Label naming: see the layout note above. Flagging in case the lead wanted Trap 24's literal
   `**Observation, not a failure:**` / `**Why it matters...**` style instead.
