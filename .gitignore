.venv/
backups/
keys/
build/
*.pem
__pycache__/
# Local credentials (NEVER COMMIT)
credentials.env
.env
.env.*
*.env
!scripts/versions.env
# labflash host artifacts (extend-only; never remove entries above)
host/**/*.egg-info/
*.pyc
*.pyo
.pytest_cache/
coverage/
# Vendor test framework (fetched, not committed)
common/labid/tests/unity/

# west-managed workspace (fetched deps, not project code)
.west/
zephyr/
# ...but the common/labid/zephyr module packaging dir IS project code
!common/labid/zephyr/

# LABID fuzz build artifacts (generated, not source)
common/labid/tests/fuzz_labid
common/labid/tests/fuzz_ubsan
common/labid/tests/fuzz_corpus/

# ESP-IDF generated config (keep sdkconfig.defaults, not the generated sdkconfig)
esp_idf/**/sdkconfig
esp_idf/**/sdkconfig.old

# ESP-IDF per-variant build dirs (BL-020: 5 variants, each its own -B dir)
# and fetched/generated component-manager artifacts (not source)
esp_idf/build*/
esp_idf/managed_components/
